from itertools import permutations

for p in permutations('0123456789', 8):
    s,e,n,d,m,o,r,y = p
    if s=='0' or m=='0': continue
    send, more, money = int(s+e+n+d), int(m+o+r+e), int(m+o+n+e+y)
    if send + more == money:
        print(send, "+", more, "=", money)
        break
